name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  # Push events stopped reaching Actions on one of the two remotes this repo is
  # pushed to, which leaves no way to get a build out. Keep a manual trigger so
  # a deploy never depends solely on the push hook firing.
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out source code
        uses: actions/checkout@v7

      - name: Set up Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"
          cache: gradle

      - name: Make Gradle executable
        run: chmod +x gradlew

      - name: Build and run tests
        run: ./gradlew clean build --no-daemon